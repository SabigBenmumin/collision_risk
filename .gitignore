# ----------------------------------------------------------------------
# 1. Output and Temporary Directories (Generated Files)
# ----------------------------------------------------------------------

# โฟลเดอร์ 'ouput' ใช้สำหรับเก็บผลลัพธ์ของการรัน (ภาพ, วิดีโอ, log)
# ไม่ควรถูก Commit เข้า Git
ouput/
# (ถ้าใช้ Jupyter Notebooks หรือ IDE)
.ipynb_checkpoints/
__pycache__/
*.pyc
*.pyo
*.pyd

# ----------------------------------------------------------------------
# 2. Dataset and Pre-trained Weights (Large Files)
# ----------------------------------------------------------------------

# โฟลเดอร์ที่เก็บวิดีโอต้นฉบับ (อาจมีขนาดใหญ่)
# แนะนำให้ใช้ Git LFS ถ้าต้องการเก็บไฟล์เหล่านี้
# แต่ถ้าไฟล์ใหญ่มากและไม่จำเป็นต้อง Track ใน Git ให้ Ignore ไปเลย
university_vedio/

# ไฟล์โมเดล (weights) ที่ได้จากการ train หรือใช้เป็น Pre-trained weights
# มักเป็นไฟล์ .pt, .pth, .h5 ซึ่งมีขนาดใหญ่
# โดยทั่วไปจะเก็บไว้ในโฟลเดอร์ 'models'
models/

# ----------------------------------------------------------------------
# 3. Environment and IDE Files
# ----------------------------------------------------------------------

# ไฟล์ Virtual Environment (ถ้าใช้)
venv/
env/
.env

# Visual Studio Code
.vscode/

# PyCharm
.idea/

# ไฟล์ Log ทั่วไปที่อาจเกิดขึ้น
*.log